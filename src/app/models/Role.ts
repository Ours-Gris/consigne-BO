export enum Role {
    USER = 'user',
    SELLER = 'seller',
    PRODUCER = 'producer',
    ADMIN = 'admin'
}

export enum openRole {
    USER = 'user',
    SELLER = 'seller',
    PRODUCER = 'producer'
}
